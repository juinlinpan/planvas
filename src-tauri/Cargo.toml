[package]
name = "planvas-desktop"
version = "0.1.0"
description = "Planvas local-first desktop shell"
authors = ["Planvas"]
edition = "2021"

[lib]
name = "planvas_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
