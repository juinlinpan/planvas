{
  "name": "@planvas/backend",
  "private": true,
  "version": "0.1.15",
  "type": "module",
  "scripts": {
    "dev": "npm run build && concurrently --kill-others -n backend-tsc,backend-node -c yellow,green \"tsc -p tsconfig.json --watch --preserveWatchOutput\" \"node --watch --watch-preserve-output dist/src/server.js --host 127.0.0.1 --port 18000\"",
    "serve": "node dist/src/server.js --host 127.0.0.1 --port 18000",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "npm run build && node dist/tests/runTests.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^24.7.2",
    "typescript": "^5.9.3"
  }
}
