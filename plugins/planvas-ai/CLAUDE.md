# Planvas AI

Use `skills/planvas-mcp/SKILL.md` when working with local Planvas projects.

Prefer the Planvas MCP server at `http://127.0.0.1:18001/sse` for live changes.
For read-only work, use the semantic XML and referenced markdown notes described in `skills/planvas-mcp/references/read-page.md`.
